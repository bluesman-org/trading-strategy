# Backtest & QA Evidence

This folder stores artifacts generated from TradingView Strategy Tester runs, alert log reviews, installation screenshots, and monitoring transcripts for the Long-Only Crypto Strategy.

## Planned Artifacts
- `btc_eur_30d.report`, `eth_eur_30d.report`, `alt_pair_30d.report`: HTML/CSV exports for regression tracking (T017, T022).
- `timeframe_switch.log`: Notes proving preset reloads during timeframe changes (T032).
- `rolling_monitoring_notes.md`, `alert_latency.md`, `rolling_pnl_90d.log`: Monitoring and latency validation (T028, T033, T035).
- `install/`: Installation QA screenshots or HTML exports per timeframe tier (T034).

Add new evidence files to this index as they are produced so reviewers can quickly locate the proof for each success criterion.
